# Dive ID iOS V0.1

Open `DiveID.xcodeproj` in Xcode 16 or newer. The app targets iOS 18 and uses only Apple frameworks. Identification and saved-species storage are deterministic, session-only mock implementations; selected photos remain in memory and are never uploaded.
